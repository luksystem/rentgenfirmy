# Plan wdrożenia etapami — Tablica Celów

> Zależność: `ARCHITEKTURA.md`, `EKRANY_I_UX.md`, `STATE_MACHINE.md`, `AI_I_METODOLOGIE.md`.
> Każda faza jest samodzielnie wydawalna i testowalna.

| Faza | Zakres | Efekt |
|---|---|---|
| **0. Fundament danych** | Migracja: `goal_board_kinds`, `goal_boards`, `goals` (z kolumnami cykliczności D4 i `parent_goal_id` D3), `goal_participants`, `goal_kpis`, `goal_updates`, `goal_comments`, `goal_reviews`, `goal_initiatives`, `goal_links`, `goal_methodologies` (schemat, **bez** seedu treści), `goal_ai_suggestions`. RLS wg `ARCHITEKTURA.md` §5 (w tym `visibility` D5). Nowa pozycja w menu „Przestrzenie” (bez UI funkcjonalnego — placeholder). | Schemat gotowy, appka działa jak dotąd |
| **1. Szkielet CRUD** | `lib/goals/types.ts`, repozytoria, `goal-store.ts`, `goal-hydrator.tsx` (layout `/tablice-celow`), API `boards`/`goals` (bez AI, bez cykliczności w UI). Ekrany #1 (hub, tworzenie tablicy) i #2 (widok tablicy, kolumny=status D2). Tworzenie celu formularzem manualnym (wybór metodologii z listy — bez dynamicznych pól). Widok celu #3 (dane podstawowe + edycja, bez KPI/przeglądów/rozliczenia). | Ręczne zarządzanie tablicami i celami |
| **2. Monitorowanie** | `progress_percent` + `goal_updates` (log), komentarze, `goal_kpis` (CRUD manualny), harmonogram przeglądów `goal_reviews` + zamykanie (guard ról D6), badge „najbliższy przegląd” na karcie. | Pełne monitorowanie bez AI |
| **3. Rozliczenie i cykliczność** | Formularz rozliczenia (`goal_settlement_form.tsx`), state machine wg `STATE_MACHINE.md` §3, automatyczne tworzenie kolejnej instancji dla `is_recurring = true` (`recurrence_parent_id`/`recurrence_root_id`). | Zamknięcie cyklu celu + wsparcie cykliczności |
| **4. Biblioteka metodologii** | Seed 9 metodologii (treść + `field_schema`), ekran #5 (lista + karta), dynamiczne renderowanie `field_schema` w formularzu tworzenia celu (zamiast statycznego wyboru z Fazy 1). | Metodologie w pełni użyteczne manualnie |
| **5. AI-doradca** ✅ | `POST /api/goals/ai/suggest` (`trigger: 'create'`), krok AI w formularzu tworzenia celu (`GoalAiAdvisorPanel`), ostrzeżenia o ogólności/niemierzalności, zapis do `goal_ai_suggestions` (audyt każdego wywołania), zastosowanie sugestii wypełnia metodologię + pola + tworzy KPI/inicjatywy/zadania/zasoby/budżet po utworzeniu celu. | Pełny flow tworzenia celu z AI |
| **6. AI w trakcie + powiadomienia** ✅ | `trigger: 'review'` w `POST /api/goals/ai/suggest` (sugestia korekty w `GoalAiReviewPanel` na zakładce „Przegląd” celu, z kontekstem: status, % realizacji, dni do końca, historia przeglądów). `lib/notifications/goal-activity.ts` — `ensureGoalActivityNotifications()` (wywoływane przy każdym wejściu do modułu, wzorem `ensureWarrantyExpiringNotifications`): `goal_review_due`, `goal_period_ending`, `goal_at_risk` (status lub tempo realizacji poniżej oczekiwanego). `goal_recurring_created` generowane bezpośrednio w `settleGoal()`. | AI i przypomnienia aktywne przez cały cykl życia celu |
| **7. Widoki zbiorcze i analityczne** ✅ | `lib/supabase/goal-history-repository.ts` (jedno zapytanie po wszystkie cele + profile, agregacje w `useMemo`). Ekran #6 `/tablice-celow/podsumowanie` (`goals-summary-dashboard.tsx`, Recharts `BarPanel`/`PiePanel`): % realizacji per typ tablicy/osoba, rozkład status/priorytet, lista celów „na czerwono” (status `at_risk` lub po terminie). Ekran #7 `/tablice-celow/historia` (`goals-history-insights.tsx`, D7): ranking dowożenia (% skuteczności per osoba), lista `not_achieved` z filtrami (tablica/poziom/okres/właściciel), aktywność ustalania celów per osoba (w tym „0 celów” dla osób nieużywających modułu), trend miesięczny % osiągniętych (Recharts `LineChart`), lista wniosków z rozliczeń. Naprawiony licznik „do przeglądu” na kafelkach huba (`fetchGoalCountsByBoard()` — batch join `goal_reviews.goal_id → goals.board_id`, `completed_at is null`). Linki z huba do obu ekranów. | Raportowanie zarządcze i wnioski organizacyjne |
| **8. Integracja kontekstowa** ✅ | Pickery projekt/klient/etap/kamień milowy w `CreateGoalDialog` (D11) — po wyborze projektu dociąga etapy/kamienie milowe wzorem `resource-plan-side-panel.tsx` (`getOrCreateProjectProcess` + anchored template), auto-uzupełnia klienta z projektu. Zakładka „Cele” w `ClientDashboardView` (tylko widok zespołu, `renderGoalsSection()` → `GoalCollectiveView` z kontekstem `projectId`/`clientId`). Link „Cele projektu” na `/projekty/[id]/proces` → `/tablice-celow/zbiorcza?projectId=...`. `GoalCollectiveView` czyta `projectId`/`clientId` z query (`useSearchParams`, strona owinięta w `Suspense`) albo z propsów (osadzenie w dashboardzie) — filtr widoczny jako usuwalny banner (poza osadzeniem w dashboardzie, gdzie jest zawsze aktywny). Nowa zakładka „Powiązania” w widoku celu (`goal-links-panel.tsx`) — wyszukiwanie i ręczne linkowanie/odlinkowywanie istniejących zadań Kanban (`goal_links`, `linked_type='kanban_task'`) przez `useKanbanCacheStore.ensureAllBoards()`, bez generowania nowych zadań. | Cele widoczne na poziomie konkretnych projektów |
| **9. Usprawnienia UX i zarządzanie treścią** ✅ | Przycisk „Pokaż/Skryj anulowane” na tablicy (dodatkowa kolumna `cancelled` w widoku). Usuwanie celu — tylko admin, z widoku celu (`removeGoal`, istniejący w store). Edycja nazwy i dat (`periodStart`/`periodEnd`) celu z zakładki „Przegląd”. Ustawienia modułu (`/tablice-celow/ustawienia`, `app_settings.goals_module_settings`) — zakres projektów w pickerach (aktywne/wszystkie); `ProjectSelectSearchable` (wyszukiwanie po nazwie projektu i kliencie, wzorem `ClientSelectWithCreate`) w `CreateGoalDialog`. Suwak % realizacji i data docelowa edytowalna bezpośrednio na kafelku celu (`GoalCard`), z kolorowaniem terminu wg zbliżania się (zielony/żółty/czerwony) — zapis przez `updateGoalQuickFields()`. Wersja „slim” kafelków + przełącznik widoku (Standard/Slim, zapamiętywany w `localStorage`). AI: `GoalAiBulkImportPanel` — wklejenie notatki → `POST /api/goals/ai/generate-batch` (`lib/ai/goal-bulk-generator.ts`) wyodrębnia wiele celów, dopasowuje metodologię, tytuł zgodny z zasadami dobrze zdefiniowanego celu; użytkownik przydziela właściciela/projekt/cykl/daty i akceptuje wsadowo. | Moduł łatwiejszy w codziennym zarządzaniu |
| **10. Asystent wyznaczania celów** ✅ | Kolumna `process_stages.description` (opis etapu „dla AI”, edytowalny w `ProcessTemplateEditor`). Ekran `/tablice-celow/asystent` (`GoalSettingAssistant`) — iteruje po aktywnych projektach, pokazuje aktywny etap procesu (z opisem) i liczbę aktywnych celów projektu (`fetchAllGoals()`), przycisk „Analizuj z AI” → `POST /api/goals/ai/assistant-suggest` (`lib/ai/goal-project-assistant.ts`) zwraca propozycje celów wraz z sugerowaną rolą/osobą, dopasowane do etapu i niepowielające już ustalonych celów; akceptacja dodaje cele do wybranej tablicy. | Proaktywne wsparcie w ustalaniu celów projektowych |
| **11. (Przyszłość, poza zakresem tego wdrożenia)** | Generowanie zadań/inicjatyw z `goal_initiatives` do `process_kanban_tasks`; pełna matryca uprawnień/widoczności (poza interim D5); integracja z modułem Problemów, gdy powstanie (`goal_links.linked_type = 'problem'`, cykl PDCA). | Rozszerzenia bez zmiany fundamentu danych |

## Uwagi do sekwencjonowania

- Fazy 0–3 dają w pełni użyteczny moduł **bez AI** — możliwe jest zatrzymanie się tutaj i ocena
  wartości przed inwestycją w Fazę 5+.
- Faza 4 (biblioteka metodologii) musi poprzedzać Fazę 5 (AI), bo AI opiera prompt na treści
  katalogu metodologii.
- Faza 7 (Historia i wnioski) wymaga zgromadzenia realnych danych z Faz 1–3 (rozliczonych celów)
  — sensowne żeby wejść w produkcję z Fazami 0–4 wcześniej, żeby zebrać dane do analiz.
